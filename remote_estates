terraform {
  backend "s3" {
    bucket = "terraform"
    key    = "states/infra.tfstate"
    region = "us-east-1"
   
  # shared_credentials_file = "~/.aws/credentials"
  # profile                 = "personal"
  }
}
